name: Test

on:
  push:
    branches:
    - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo $DATE_RUN
        env:
          DATE_RUN: ${{ $(date '+%Y-%m-%d') }}
